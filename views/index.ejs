<!DOCTYPE html>
<html lang="en">

<%- include('template/header.ejs') %>

    <body>
        <%- include('template/navbar.ejs') %>
            <div class="container">
                <div class="row">


                    <% for (rip of arp){ %>
                        <div class="col-xl-3 col-lg-4 col-md-6 col-sm-12">

                          
                                    <div class="card text-center" style="width: 18rem; margin: auto;">
                                        <img src="<%= rip.img %>" class="card-img-top" alt="...">
                                        <div class="card-body">
                                          <h5 class="card-title"><%= rip.name %></h5>
                                          <a href="<%= rip.link %>" class="btn btn-primary">Download</a>
                                        </div>
                                      </div>
                        </div>
                        <% } %>
                </div>
            </div>
    </body>

</html>